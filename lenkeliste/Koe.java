public class Koe<T> extends Lenkeliste<T> {
    /*
    Her passerer alle testene, og det er ingen metoder som har behov for å redefineres.
    Dette er fordi Lenkeliste fungerer som en kø, altså det er First-In-First-Out (FIFO) prinsippet som gjelder.
    Én måte å tenke på det er for eksempel en kø ved kassa på en butikk;
    kunden som står først/fremst i køen er den første kunden til å få behandling.
    */
}
